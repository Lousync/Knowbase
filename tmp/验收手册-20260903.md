# Knowbase 验收手册（2026-09-03 · 分支 fix/optimize-v2.15.1）

> 范围：今天所有代码已就绪、沙箱内无法真机验证、需要你在实例里逐项验收的工作。
> 前置：`npm run dev` 启动（当前仓库 E:/knowledge 已迁移完整：216 页 / 36 博客 / 9 标签）。
> 建议顺序：先验 A 图谱 → B 编辑器 → C 插件 R7。每组末尾留「通过/失败 + 现象」记录位。
> 若某步失败，记录现象后继续，别卡住；结束把失败项给我即可定位。

---

## A. R4 图谱（G1-G3 + A8 增删动画）

代码位置：`src/modules/knowledge/components/graph/`（GraphView/GraphCanvas/forceSim）+ 知识模块入口。
已含：G0 GraphIndex 数据层、G1 画布+跳转、G2 交互+本地图谱、G3 设置持久化/过滤/簇力/虚节点、A8 增量 merge。

### A1 基本进入与画布
- [X] 知识模块 → 左侧分类树**底部**「图谱」按钮 → 进入全幅画布
- [X] 左上角显示统计「X 页面 · Y 标签 · Z 关联」（真库应为 216/9/153 量级）
- [X] 节点自动散开收敛后**停帧**（CPU 降下来，无持续动画）
- [ ] 空仓库/未开仓库 → 空态提示不崩

### A2 交互
- [X] 滚轮缩放（以光标为中心）、空白拖拽平移、松手有惯性
- [X] 拖一个节点：邻居被带动、松手回弹
- [X] hover 节点：邻居高亮过渡 ~200ms，其余淡化
- [ ] 单击页节点 → 右上卡片 →「在编辑器中打开」→ 跳到编辑器 Tab 打开对应 .md   有问题，只会跳转到编辑器页面，但是并不会打开具体的页面。并且再添加一个[在阅读器中打开]的按钮
- [ ] Esc 依次关卡片 → 关设置 → 退出图谱回列表

### A3 设置与过滤（右上「设置」）
- [ ] 连线长度/斥力滑杆即调即生效
- [ ] 显示标签节点 / 显示孤立页 / 主题成团(簇力) 开关即时生效
- [ ] 重启应用后设置保留（`E:/knowledge/.knowbase/config.json` 出现 graphView 段）

### A4 本地图谱
- [ ] 点卡片「查看它周围的页面」→ 局部子图从该页"长出来"
- [ ] 左上出现「本地图谱 · X（点此回全图）」→ 点击回全图

### A5 【A8 核心】增删页面局部更新（重点验）
- [ ] 编辑器里新建一个带 `[[链接]]` 的页面并保存（或删一个被引用的页）
- [ ] 切回知识 Tab（图谱还开着）→ 图谱自动刷新：新页在原位附近**生长淡入**、删掉的页**收缩淡出**
- [ ] 其余节点**不重排、视角不跳**
- [ ] 若数据没变（纯正文修改保存）→ 切回图谱**无任何闪烁**
- 结果记录：__________

---

## B. 编辑器增强（回跳闭环 + 淡化 + 补全 + 大纲）

代码：`src/modules/editor/`（MonacoPane 双轨、markdownDim/markdownOutline 纯函数）+ `src/components/shared/PluginFrame`（插件部分归 C）。
已含：知识库↔编辑器双向跳转、Markdown 标记淡化（档 A）、`[[` 补全、大纲导航、wiki 常驻高亮。

### B1 知识库 → 编辑器（沿用旧入口，回归）
- [ ] 知识库页面工具栏「编辑」按钮 → 切编辑器 Tab 打开同一文件
- [ ] 图谱卡片「在编辑器中打开」、Ctrl+O 快速切换、Ctrl+` 全局搜索命中 → 均能进编辑器

### B2 编辑器 → 知识库（新回跳闭环，重点验）
- [ ] 编辑器打开任一知识页 .md → 顶部工具栏「**在知识库中阅读**」按钮
- [ ] 干净文件直接点 → 切知识库 Tab 并**定位打开同一页**
- [ ] 有未保存修改时点 → 弹「先保存？」确认框（保存并查看 / 不保存直接查看）
- [ ] 选不保存 → toast 提示「未保存，显示磁盘旧内容」→ 仍跳转打开
- [ ] 文件右键菜单也有「在知识库中阅读」
- [ ] 保存后切回：能读到最新内容（索引失效 + 激活重读链路生效）
- 结果记录：__________

### B3 Markdown 标记淡化（设置默认开）
- [ ] 编辑器打开 .md：非光标行的 `**` `#` `-` `>` `[x](u)` `[[x]]` 标记变淡、被包裹内容有强调（加粗/斜体/链接色）
- [ ] **光标所在行恢复原始标记**，编辑无干扰
- [ ] 设置 → 编辑器 → 关「Markdown 标记淡化」→ 立即恢复纯文本（仅双链仍 accent）
- 结果记录：__________

### B4 `[[` 双链补全（重点验）
- [ ] 编辑器 markdown 中敲 `[[` → 弹出知识页候选（标题模糊匹配）
- [ ] 选择 → 自动补成 `[[页名]]`
- [ ] 补全出的双链保存后 → 图谱/反链能识别（回 B/A 验证闭环）
- 结果记录：__________

### B5 大纲导航
- [ ] 工具栏「大纲」按钮（仅 md 显示）→ 右上浮层列出 # 标题树（缩进按层级、行号）
- [ ] 代码块内的 `#` 不误当标题
- [ ] 点击条目 → 跳到对应行并聚焦
- [ ] 点编辑器空白处浮层收起
- 结果记录：__________

---

## C. R7 插件开放（v3：执行但关沙箱）

代码：`electron/lib/pluginHostGateway.ts`（新建）+ `pluginRegistry.ts` + `src/components/shared/CodePluginHost(s)` + manifest 校验。
已含：V3-1 契约（文档+校验）、V3-2a 网关骨架、V3-2b code 放行、V3-2c UI/Worker 宿主、V3-2d 自动挂载、V3-3 code-hello 试点。

### C1 存量插件回归（先确认没打破旧世界）
- [ ] 插件页能列出已装插件（错题本等），启用/禁用/卸载正常
- [ ] 打开一个**存量 UI 插件**（如错题本视图）→ 功能正常（v1 协议兼容分支生效）

### C2 插件管理 UI 的 code 呈现
- [ ] 插件详情「更多信息 → 类型」三态文案正常：UI 插件(带界面)/代码插件(后台运行)/声明式插件(纯内容)
- [ ] 代码插件的「能力与授权」区可显示（若有能力声明）

### C3 【核心】code-hello 端到端（重点验，V3-3 试点）
- [ ] 插件页「安装内置示例」→ 选 **code-hello**（samples/code-hello-0.1.0.zip）→ 装成功
- [ ] 详情显示「类型：代码插件(后台运行)」
- [ ] 点**启用** → 数秒内应弹出 toast「**Code 插件已启动（worker 探针 OK）**」
  - toast 出现 = Worker 被 CodePluginHosts 自动挂载 + v2 协议 + Gateway 免授权裁决 + 宿主渲染 toast 全链路通
- [ ] 开发者工具 Console 可见 `[code-hello] worker 脚本已加载…` / `worker 就绪` 日志
- [ ] 点**禁用** → Worker 终止（Console 无报错泄漏）
- [ ] 重启应用（保持启用）→ 启动后仍弹 toast（自动挂载随启动恢复）
- 结果记录：__________

### C4 v2 网关安全性抽查（可选，配合 C3）
- [ ] 启用期间插件行为审计有记录（插件页 → 最近活动 / 审计）
- [ ] 若 code-hello 停用后再发请求 → 应被拒（EDISABLED），无异常泄漏

---

## D. 顺手回归（被重构触碰的邻近面）

- [ ] 全局搜索（Ctrl+`）仍可用且能打开文件
- [ ] 命令面板（Ctrl+Shift+P）/ 快速切换（Ctrl+O）正常
- [ ] 知识库沉浸阅读（Ctrl+Shift+R / Esc）没被图谱改动破坏
- [ ] 编辑器 Ctrl+S 保存、Ctrl+Shift+S 全部保存、frontmatter「属性」弹窗正常

---

## E. 图谱目录 scope + 分类树修复（7e9ee82 + bd2b72f）

> 前提 bug 已修：categories.json 是 dict（此前按 array 解析 → 76 分类全读空，分类树可能一直是空的）。修复后树应完整显示。

- [ ] **分类树回归**：知识库左侧分类树能完整展开（3 空间 / 10 笔记本 / 63 文件夹）
- [ ] **目录 scope（核心）**：选中「学习空间 → 某笔记本」（任意层级含章节）→ 底部点「图谱」→ 图谱**只显示该目录（含子目录）的页面** + 与它们有连接的跨目录外部页
- [ ] 左上角出现目录 chip（如「学习空间/数据结构」）；点 ✕ → 回全库（原 225 节点）
- [ ] 未选中任何目录点图谱 = 全库（原行为不变）
- [ ] scope 内增删页 → 局部动画不整图重排（与 A5 同链路）
- 结果记录：__________

---

## F. 编辑器 R5 分栏预览（13439ad）

- [ ] 编辑器打开 .md → 工具栏出现「预览」按钮（仅 markdown 文档有）
- [ ] 点开 → **左 Monaco / 右实时渲染 50/50**；右侧随编辑实时更新（大文档打字不卡）
- [ ] 预览内 `[[双链]]` 可点击跳文件（未展开目录会提示）
- [ ] 关闭预览再重开编辑器 → 保持上次状态（localStorage 记忆）
- [ ] 预览渲染与知识库沉浸阅读效果一致（同一 MarkdownPreview 引擎）
- 结果记录：__________

---

## G. PDF 阅读器 P1 · 内置阅读器（594d426 → c0ab7f9 → f86e136 → 77c540c）

> **⚠️ 口径标注：这是「内置阅读器」，不是插件**。
> 按 plugin-pdf-reader-design 分期：**P1 = 编辑器组文档类型**（编辑器打开 .pdf 走内置 `PdfReaderView`，
> 与 .md 同路径，非插件市场形态）；**「PDF 阅读插件」= P2**（外壳迁为官方 UI 插件），尚未做。
> G 组验收的是 P1 内置阅读器功能本身。相关但不在此组：`files` 能力（插件弹对话框读文件）是
> 插件体系扩展，插件形态的 PDF 验收要等 P2。

> 链路：知识库附件条 → 编辑器 PdfReaderView（懒加载 range → 大纲/搜索/文本层/沉浸）。

- [ ] **编辑器直开**：编辑器文件树找仓库里的 .pdf（或任何无扩展名 PDF）→ 双击 → 渲染页面而非乱码文本（懒加载，超大 PDF 不卡）
- [ ] 工具栏：翻页 / 页码输入 / 适宽 / 缩放 ± 百分比
- [ ] **大纲**：有书签的 PDF → 左侧「大纲」面板列出书签树 → 点击跳页
- [ ] **Ctrl+F 搜索**：搜词 → 命中列表（页码+片段）→ 点击跳页且该页命中词高亮
- [ ] **文本层**：页面文字可鼠标选中复制
- [ ] **沉浸**：点「沉浸」→ 工具栏/侧栏隐藏、页面居中、底部悬浮条 30s 淡出、动鼠标唤出；Esc 退出
- [ ] **附件路由**：知识库打开带 PDF 附件的页 → 沉浸阅读标题下附件条 → 点「阅读」→ 编辑器打开该 PDF
- 结果记录：__________

---

## H. R6① 老库导出工具（b5af87a，可选数据安全验证）

- [ ] 终端跑：`node scripts/migrate-db-to-vault.mjs --db "C:/Users/徐志岩/AppData/Roaming/knowbase/data/knowledge.db" --out "<仓库>/.knowbase/legacy-export" --force` → 47 表导出 + 「全部校验通过」
- [ ] 二次跑不加 --force → 被拒（exit 2），数据安全网生效

---

## I. 工具箱插件化（222dafc + 9817f24）

> 口径：工具箱插件化第二批——插件工具宿主升级 PluginFrame v2 + 首个真实迁移对象 kb.weight-tracker。
> 数据走 kb.store（`<仓库>/.knowbase/plugins/<pluginId>/records.json`，原子写可人工读）。

- [ ] **存量回归（宿主升级不破）**：工具箱「插件工具」里强密码生成器（已装）→ 点开正常生成/复制（v1 兼容分支）
- [ ] **装体重插件**：插件页「安装内置示例」→ 选 kb.weight-tracker → 详情显示「UI 插件」+ 零能力
- [ ] 启用后工具箱出现「插件工具 → 体重」卡片 → 点开进入记录页
- [ ] 录入：今天日期自动、输 kg 值 + 备注 → 「记录」→ 列表出现；**同一天再录 → 覆盖不新增**
- [ ] 再录几天的数据（改日期）→ **曲线出现**：网格刻度、日期轴、折线 + 点；hover 点 → tooltip 显示日期/体重/备注
- [ ] **kg / 斤 切换**：数字 ×2、统计卡与曲线刻度联动
- [ ] 统计卡：最近 / 最低 / 最高 / 较首日变化（涨红跌绿）数值正确
- [ ] 撤销（删最近一条）与逐条删除生效
- [ ] **数据可人工读**：打开 `<仓库>/.knowbase/plugins/kb.weight-tracker/records.json` → 是格式化 JSON，记录与界面一致
- [ ] 浅色/深色主题下插件配色跟随（主题变量注入）
- 结果记录：__________

---

## J. 清空全部数据（ef7d606）

> **前置**：先用全仓备份（工具箱 → 数据导出）拷一份到别处。这是真删磁盘文件，不可撤销。
> 重写后语义对齐去库化：仓库内容文件（.md / 目录 / 附件）真删 + .knowbase 整体重建骨架 + 回首启引导。
> 历史 bug：旧实现只 DROP sqlite 空表 → 「文件不删 / 列表空(vault id 丢) / 图谱仍有旧节点」三不一致已修。

- [ ] 设置 → 危险区「清空全部数据」→ 文案列表已含「当前仓库内的全部内容文件」「回首启引导」等真实反映
- [ ] **护栏测试（前端不可验）**：仓库路径校验 isAllowedClearRoot 拒 C:\Users / 盘符根（见冒烟 21/21）
- [ ] 输入确认短语「永久清空全部数据」→ 「确认清空」可点
- [ ] 清空后：当前仓库根下非隐藏内容（.md 知识、博客目录、附件）**磁盘文件已删**
- [ ] 清空后：`.knowbase/cache/knowledge-index.json` 与 `graph.json` **已被删**（关键——旧实现不删导致图谱显示旧节点）
- [ ] 清空后：`.knowbase/` 重建骨架（`meta.json` / `_inbox/` / `_attachments/`）
- [ ] 清空后：仓库根隐藏项 `.gitignore` / `.obsidian` 等**保留**（不误删用户私有隐藏文件）
- [ ] 应用重回**首启引导**（重新选/建仓库）—— 不是回到「空列表但有旧缓存」的诡异状态
- [ ] 重新进入一个仓库后写入新页面 → 知识库列表 / 图谱 / 文件三者一致
- 结果记录：__________

---

## K. 编辑器主题跟随（Monaco 编辑区配色）

- [ ] **深色主题下**：编辑器编辑区背景跟随深色（不再是白底），行号/光标/选区配色协调（@monaco-editor/react 强制 theme="knowbase-auto"）
- [ ] **浅色主题下**：编辑器编辑区背景跟随浅色，行号/光标等配色协调
- [ ] **主题切换时**：编辑区主题实时跟随（MutationObserver 自动重应用），无需重启
- [ ] 切回原主题历史 bug 描述：截图显示应用外壳深色但编辑器大块白底 → 已修
- 结果记录：__________

---

## 验收结果汇总

| 组 | 通过数 | 失败项（现象） |
|---|---|---|
| A 图谱（含 A8 动画） | / | |
| B 编辑器（回跳/淡化/补全/大纲） | / | |
| C 插件 R7（V3-2~V3-4 + code-hello） | / | |
| D 顺手回归 | / | |
| E 图谱目录 scope + 分类树 | / | |
| F 编辑器分栏预览 | / | |
| G PDF 阅读器 P1 | / | |
| H R6① 导出工具 | / | |
| I 工具箱插件化（体重插件） | / | |
| J 清空全部数据（ef7d606） | / | |
| K 编辑器主题跟随 | / | |

**验完把失败项发我**——每项都有对应代码位置与提交，可快速定位。
