---
title: 核心概念：读写分工与草稿机制
category: 入门
icon: Split
weight: 5
keywords: [读写分工, 草稿, frontmatter, id, 看不到, 知识库看不到, 为什么没有, 收录, 草稿机制, md, 元数据]
---

# 核心概念：读写分工与草稿机制

**一句话**：编辑区是唯一能写正文的地方，知识库只负责阅读与组织；而知识库只收录带 `id` 的文件，其余的算草稿。

这一页解释了新用户最容易撞墙的两件事，读完能省下大量困惑。

## 一、读写分工

| | 编辑区 | 知识库 |
|---|---|---|
| 职责 | **写**：新建、编辑、重命名、移动、删除文件 | **读**：阅读、双链、标签、图谱、搜索 |
| 操作对象 | 磁盘上的 `.md` 文件（仓库里的真实文件） | 已收录页面的索引与元数据 |
| 能不能新建 | 能（这是唯一入口） | **不能**（这是有意设计，不是缺失） |

所以：**在知识库里找不到「新建」按钮是正常的** —— 去编辑区建文件，写完它自己会出现在知识库。

## 二、草稿机制：什么样的文件才算「知识库页面」

知识库只收录**带 frontmatter `id` 的 .md 文件**。没有 `id` 的文件被当作**草稿**：

- 草稿只在编辑区可见，知识库一律不显示；
- 草稿不算「丢了」，文件实实在在躺在仓库里；
- 草稿变成页面只需补三行：

```yaml
---
id: my-first-note
title: 我的第一篇笔记
---
```

`id` 全库唯一、**一旦确定不要再改**（改名会导致双链与反向链接失配）。`title` 只是显示名，随便改。

> **为什么这么设计？** 让你可以随便写、随便存 —— 临时记的片段、没写完的半成品不会把知识库塞满。只有当你明确「这篇要进知识库」时才正式收录。

## 三、自查三步：写了文件但知识库看不到

1. 打开那个文件，看开头有没有 `---` 包裹的 frontmatter 区块；
2. 确认区块里有 `id:` 这一行，且值不为空；
3. 保存后回知识库，正常会立刻出现（写盘后会广播刷新）。

还是看不到，再看是不是**落在被忽略的目录里**（仓库根的 `.ignore` 规则，gitignore 语法）。

## 常见误用

- 只在正文写了标题、没加 frontmatter → 属于草稿，符合预期；
- 字段名写成 `ID` / `Id` → 不识别，**字段名区分大小写**；
- `id` 留空或写了空格 → 视为无效，仍算草稿；
- 把 `id` 改成中文或带斜杠的路径 → 虽然能存但容易在双链解析时出问题，建议用英文 + 连字符；
- 以为「知识库看不到 = 文件丢了」，于是重新写一遍 → 结果仓库里出现两份；
- 用系统资源管理器直接改了文件，但软件里的列表没刷新 → 切一下模块或重启即可（索引按需重建）。

## 相关

《快速上手》 · 《编辑器文件管理》 · 《双链与标签》 · 《常见问题》
