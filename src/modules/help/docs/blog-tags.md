---
title: 博客标签
category: 操作指南
icon: Tag
---

标签用于对博客文章进行分类。每篇文章最多可添加 **5 个标签**。

## 添加标签

1. 打开任意一篇博客进入编辑器
2. 在工具栏下方的标签栏点击 **+ 标签**
3. 输入标签名称，按 Enter 确认
4. 标签会自动保存，显示在博客卡片上

## 删除标签

点击标签右侧的 ✕ 按钮即可移除。标签仅从当前文章中移除，不会删除标签本身。

## 注意事项

- 同一标签可在多篇文章中使用
- 标签名不可重复（系统层面）
- 博客卡片最多显示 3 个标签，超出部分显示 `+N`
