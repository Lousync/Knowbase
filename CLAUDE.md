@AGENTS.md

# Claude Code 附加说明

本文件只做 import 与 Claude 特有约定。**项目规则的唯一维护处是 `AGENTS.md`** —— 要改规则请改那一份，这里不重复内容。

- 本机私有约定（绝对路径、跨仓库操作、沙箱手法）见 `CLAUDE.local.md`，它不进版本控制。
- 本项目另有其他 agent 工具在用（Codex / Cursor 等）。改 `AGENTS.md` 时请保持**工具中立**：不写某一家才有的语法或路径。
- 会话级临时约束（某次会议的要求）写在对话里即可，不要沉淀进 `AGENTS.md`。

## 两条 Claude Code 侧的本地须知

- **`@` 导入是启动时全量展开**：`@AGENTS.md` 的内容会随本文件一起进上下文。拆文件**不会**减少上下文占用——它的价值是「一处改、处处对」，别指望靠拆分省 token。
- **`@` 相对路径以本文件所在目录为基准**（不是工作目录），所以这里写同目录的 `@AGENTS.md` 最稳。
